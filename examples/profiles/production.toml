[profile]
name = "production"
# Empty means all built-in and explicitly allowlisted installed plugins.
enabled = []
disabled = []

# Example operator-owned plugin configuration:
# [plugins."company.audit-events".config]
# topic = "evoagent.review.audit.v1"
# timeout_seconds = 3
