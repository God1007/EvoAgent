# Exactly one route is accepted. Secrets are resolved from the named
# environment variable at startup.
[[routes]]
id = "primary"
provider = "openai-compatible"
model = "review-model"
base_url = "https://model.example/v1"
api_key_env = "MODEL_API_KEY"
region = "eu-west"
